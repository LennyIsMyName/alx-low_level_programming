#include <string.h>

/**
 * _strcpy - copies strings to strings
 * @dest: copy to this
 * @src: copy from this
 * Return: the pointer to dest
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (0);
}
