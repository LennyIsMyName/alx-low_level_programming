#include <stdio.h>

/**
 * main- entry point
 * Return: zero
 *
 * print 0 to 9 separated by space.
 * print 0 before the digits
 * separate them with space.
 */

int main(void)
{
	int i = 0;
	while (i <= 9)
	{
		putchar(0);
		i++;
	}
	return (0);
}
